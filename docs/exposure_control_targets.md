# Exposure Control Loss ($L_{exp}$) Calibration

Parameters to prevent frame overexposure:
- Computes mean absolute distance to exposure constant $E$.
- Exposure target $E$ defaults to $0.6$ based on visual dataset benchmarks.
