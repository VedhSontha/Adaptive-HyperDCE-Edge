# DCE-Net Convolutional Layers

Deep learning layers mapping frames to light-curves:
- Network: 7-layer convolutional neural network (DCE-Net).
- Filters: symmetric $3\times3$ kernels preserving spatial coordinates.
- Skip connections: merges early features with late estimations to prevent detail losses.
